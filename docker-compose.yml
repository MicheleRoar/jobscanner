services:
  job-monitor:
    build: .
    container_name: job-monitor
    ports:
      - "5000:5000"
    environment:
      - TZ=Europe/Zurich
    volumes:
      - ./data:/app/data
    env_file:
      - .env
    restart: unless-stopped
